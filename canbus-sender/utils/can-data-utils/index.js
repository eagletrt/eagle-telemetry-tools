const defaultCanData = require('./defaultCanData');
const updateCanData = require('./updateCanData');
const purgeCanData = require('./purgeCanData');

module.exports = {
    defaultCanData,
    updateCanData,
    purgeCanData
};